## Usage

To use this development environment you will require:
- make
- docker
- docker-compose

All commands are available using `make help` at the root of this repository.

To build all the containers and populate database you can use `make kick-off`

A superuser can be created using `make createsuperuser`

